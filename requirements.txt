# =============================================================================
# Semi Senti - Runtime Dependencies
# -----------------------------------------------------------------------------
# - Python 3.8+ 호환 버전으로 핀(pin) 했습니다.
# - 표준 라이브러리(sqlite3, logging, dataclasses 등)는 별도 명시하지 않습니다.
# - Phase 별로 그룹화하여 점진적 설치가 가능하도록 정리했습니다.
# - 변경 시 PRD §4.2 기술 스택 표와 동기화하세요.
# =============================================================================

# ----- Core (Phase 1 ~ 모든 모듈 공통) ---------------------------------------
python-dotenv==1.0.1           # .env 로드 (PRD §4.2 / 사용자 룰: 하드코딩 금지)
requests==2.31.0               # 외부 API 호출 (DART, 네이버 등)
pandas==2.1.4                  # 3.12 wheel 지원 (2.0.x 는 3.12 미지원)
numpy==1.26.4                  # pandas 2.1.x + Python 3.12 호환
python-dateutil==2.9.0.post0   # 날짜 파싱 유틸리티

# ----- Phase 1: 데이터베이스 (PostgreSQL) ------------------------------------
psycopg2-binary>=2.9.9         # PostgreSQL 드라이버 (F-1.3, PRD §4.2)

# ----- Phase 1: 데이터 수집 ---------------------------------------------------
pykrx>=1.0.45                  # KRX·네이버 기반 일봉 OHLCV (F-1.1.2, 무료·API 키 불필요)
beautifulsoup4==4.12.3         # HTML 파싱/정제 (F-1.2.2)
lxml==5.2.2                    # bs4 파서 백엔드 (속도/안정성)

# ----- Gemini API (F-3.3) -----------------------------------------------------
google-generativeai>=0.7.0     # Gemini API 클라이언트

# ----- Phase 2: NLP 분석 엔진 -------------------------------------------------
# JDK 1.8+ 사전 설치 필요 (PRD §4.2 런타임 의존성)
konlpy==0.6.0                  # 형태소 분석 (F-2.1)
JPype1==1.7.1                  # KoNLPy JVM 브리지 (3.12 win wheel)

# ----- Phase 4: 알림 ---------------------------------------------------------
python-telegram-bot==20.7      # 텔레그램 알림 (F-5.1)

# ----- FastAPI (HTTP API · on-demand 수집) -----------------------------------
fastapi==0.111.1               # HTTP API (python -m semi_senti.api)
uvicorn[standard]==0.30.1        # ASGI 서버

# ----- Dev / Test -----------------------------------------------------------
pytest==7.4.4
pytest-cov==4.1.0
