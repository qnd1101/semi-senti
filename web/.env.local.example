# Semi Senti — Web (.env.local)
# 본 파일을 `.env.local` 로 복사하여 사용하세요.

# 백엔드 SQLite 파일 경로 (레포 루트 기준)
# Route Handler가 read-only 모드로 직접 조회합니다.
SEMI_SENTI_DB_PATH=../db/semi_senti.sqlite

# 자동 갱신 주기 (초). PRD §F-4.2.4 = 5분.
NEXT_PUBLIC_AUTO_REFRESH_SECONDS=300
